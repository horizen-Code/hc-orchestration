# 🚀 HC Microservices Orchestration

    Этот репозиторий объединяет все микросервисы проекта с использованием Git Submodules.

# 🛠 Быстрый старт

## Первое клонирование (для новых разработчиков):

```
git clone --recursive https://github.com/horizen-Code/hc-orchestration

cd hc-orchestration

docker-compose up -d
```

## Если уже клонировали без --recursive:

```
git submodule init

git submodule update
```

# 🔄 Работа с микросервисами

## Каждый сервис — отдельный репозиторий:

- Разработка в сервисе: cd services/hc-auth-service

- Коммиты делаются в репозитории сервиса

- PR создаются в репозитории сервиса

# 📝 Важные правила

1. Работайте в своем сервисе → делайте коммиты в его репозитории

2. После мержа в main сервиса → обновите submodule в основном репозитории

3. Не коммитьте изменения в папках services/ — они управляются через submodules

## Команды для работы с submodules:

```
# Обновить конкретный сервис
git submodule update --remote services/hc-auth-service

# Перейти в сервис и работать как с обычным репо
cd services/hc-auth-service
git status
git add .
git commit -m "Your changes"
git push origin main

# Вернуться и обновить ссылку
cd ../..
git add services/hc-auth-service
git commit -m "Update auth service to latest"
```

## 💡 Помните: Этот репозиторий — только для оркестрации. Основная разработка происходит в репозиториях отдельных сервисов!
